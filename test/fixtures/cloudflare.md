# Cloudflare Workers

Bindings are available from `c.env` inside a handler.

## Deployment

Run `npm run deploy` to execute the configured deployment script.

## Runtime scope

Cloudflare bindings and static asset settings are runtime-specific and should not be copied unchanged to Node.js.
