# Autonomous Build Note

This phase is executed under `docs/roadmap/remaining-phases.md`. Every slice targets `main`, requires green CI, is squash-merged, and is verified from the new `main` head before the next slice begins.
